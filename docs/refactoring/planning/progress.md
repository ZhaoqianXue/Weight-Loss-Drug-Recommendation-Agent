# 工作记录

## 2026-09-14
- 已读取 README、TODO、忽略规则及目录清单。
- 已检查上级 AGENTS.md 和技能恢复脚本，无额外输出或恢复上下文。
- 接下来检查路径耦合、版本数据组织、测试覆盖和公开实践。
- 已核实历史 annotation seed 和当前 standardization 模块依赖，明确不可直接整体归档。
- 数据校验 passed；Python 15 项测试通过；JS 3963 项检查通过。
- 已编写 docs/architecture-refactoring-plan.md，包括目标树、完整迁移映射、P0–P4 与验收边界。

## 实施进度
- 已冻结 Git revision、130 个受跟踪文件哈希和原测试输出；112 项迁移映射保存于 docs/refactoring。
- 已完成 src 包、配置、CLI、新运行目录、冻结保护和网页构建；91 项要求按字节保留的文件审计通过。
- 修复迁移后测试仍引用旧模块路径与旧 catalog 路径的问题，原 15 tests / 3963 checks 再次通过。
- 新增合成数据回放流程，第一次本地运行通过；正在补充结构回归和干净环境验证。

## 完成
- P0–P4 已实施，未改写 Git 历史或执行在线模型/数据库操作。
- 两个干净环境、wheel 安装和所有已列验收通过，详情写入 docs/refactoring/implementation.md 与 verification.json。
- 依赖安装时解决了旧 macOS 版本报告问题；重型库首次导入超时，延长上限后模拟测试全部通过。

## P5 收尾执行
已移除六个 code_* 根目录；拆分测试；迁移规划/QA 文档；删除临时 wheel 环境；目录审计已加入 Makefile 与 CI。接下来执行最终回归。

P5 验收完成：目录审计、114 项迁移映射、91 个冻结字节检查通过；27 项离线测试、5 项研究适配层测试和 3963 项前端检查全部通过。最终根目录只保留规划的源码职责目录；本地环境与缓存为隐藏目录。
