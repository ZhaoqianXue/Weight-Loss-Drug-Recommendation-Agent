# Generated retrieval indexes

`weightloss build-index` writes dataset- and model-fingerprinted indexes to the selected run's subdirectory. No current index was built by the structural migration. Old stale indexes are preserved under `archive/indexes/` and are not treated as valid current outputs.

Run subdirectories are ignored by Git; this README preserves and explains the artifact boundary in a fresh checkout.
