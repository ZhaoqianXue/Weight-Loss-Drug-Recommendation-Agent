# Notebooks

`legacy/` retains the two original exploratory notebooks with cleared outputs. They are historical references, not the maintained execution pipeline. Their old file references can be resolved through `docs/refactoring/migration-map.json`; references to absent FAERS spreadsheets remain unavailable. Use the installed `weightloss` package for new analysis and save experimental inputs/parameters in a run manifest.
